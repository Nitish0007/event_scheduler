<%= render partial: 'shared/form', 
  locals: { 
    form_resource: @event, 
    form_submission_path: "/users/#{current_user.id}/events/#{@event.id}", 
    submit_button_title: "Update Event",
    form_method: :put,
    form_title: "Update Event",
    form_fields: [
      { type: "hidden", name: "user_id", value: current_user.id },
      { type: "text", label: "Event Title", name: "event_title", value: @event.event_title },
      { type: "text", label: "Event Venue", name: "event_venue", value: @event.event_venue },
      { type: "datetime", label: "Event Date & Time", name: "event_date", value: @event.event_date, options: {min_date: Date.today, max_date: Date.today + 1.year, min_time: Time.now} }
    ],
    actions: {
      back: {
        title: "Back",
        path: "/users/#{current_user.id}/events"
      }
    }
  }
%>